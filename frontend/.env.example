# Only if /api requests fail (e.g. not using Vite dev server). No trailing slash.
# VITE_API_BASE=http://127.0.0.1:8010

# Vercel production: leave unset so the browser uses same-origin /api (see README "Deploying to Vercel").
